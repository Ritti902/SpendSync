name: Keep Alive

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  keep-alive:
    runs-on: ubuntu-latest
    steps:
      - name: Ping SpendSync Service
        run: |
          response=$(curl -s -o /dev/null -w "%{http_code}" https://YOUR-RENDER-URL.onrender.com/api/v1/health)
          echo "Response: $response"
          if [ "$response" = "200" ]; then
            echo "✓ Service is alive"
          else
            echo "✗ Service returned $response"
            exit 1
          fi
